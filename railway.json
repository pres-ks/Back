{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "dotnet restore && dotnet publish -c Release -o output"
  },
  "deploy": {
    "startCommand": "dotnet output/ConsumoAPI2.Api.dll --urls \"http://0.0.0.0:8080\"",
    "restartPolicyType": "ON_FAILURE"
  }
}
